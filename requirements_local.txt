# 本地依赖（不需要 GPU）
# 安装命令：pip install -r requirements_local.txt

# Step 1：数据生成
openai>=1.0.0

# Step 2：K-means 聚类筛选
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
numpy>=1.24.0

# Step 4：评测（模拟模式不需要 torch）
# 如需真实推理，另安装：pip install torch transformers peft

# Step 5：Gradio 演示界面
gradio>=4.0.0
